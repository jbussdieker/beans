<%= simple_form_for(@account) do |f| %>
  <%= f.input :account_type_id, collection: AccountType.all.collect {|a| [a.name, a.id]} %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
